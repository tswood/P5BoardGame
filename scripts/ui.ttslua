function getBasicPlayerButtonParams(options)
  local template = {
    function_owner = nil,
    rotation = {0, 180, 0},
    width = 400,
    height = 100,
    font_size = 50
  }

  merge(options, template)

  return template
end
